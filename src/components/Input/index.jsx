import React from "react";

export default function Input({ className }) {
    return (
        <div>
            <label htmlFor="#input"></label>
            <input id="input" />
        </div>
    );
}
